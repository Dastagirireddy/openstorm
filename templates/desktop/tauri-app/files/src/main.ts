// Tauri app entry point
console.log("Hello from {{project-name}}!");

// Example: Invoke a backend command
// import { invoke } from "@tauri-apps/api/core";
// await invoke("greet", { name: "World" });
